apiVersion: v1
kind: Service
metadata:
  name: {{ include "spool.fullname" . }}
  labels:
    {{- include "spool.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spool.selectorLabels" . | nindent 4 }}
