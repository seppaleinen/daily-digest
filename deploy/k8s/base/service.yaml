apiVersion: v1
kind: Service
metadata:
  name: daily-digest-api
  namespace: daily-digest
spec:
  type: ClusterIP
  selector:
    app: daily-digest
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
