apiVersion: v1
kind: ConfigMap
metadata:
  name: spool-config
  namespace: default
data:
  INFERENCE_PROVIDER_URL: "http://whisper-service.default.svc.cluster.local"
  WHISPER_MODEL: "whisper-base"
  SUMMARIZATION_MODEL: "mistral"
  DIGEST_API_URL: "http://digest-api.default.svc.cluster.local"
