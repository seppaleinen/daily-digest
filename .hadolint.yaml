---
# Hadolint configuration
# https://github.com/hadolint/hadolint#configure
ignored:
  # DL3018: Pin versions in apk add.
  # Our build stages install python3, make, g++ for better-sqlite3 native compilation.
  # Alpine version pinning is fragile and unnecessary for build tools in ephemeral stages.
  - DL3018
